import { Stream } from './stream';
/**
 * @hidden
 */
export declare class Result {
    private value;
    private rest;
    constructor(value: any, rest: Stream);
    map(fn: Function): Result;
    chain(fn: Function): Result;
    fold(s: Function, _?: Function): Result;
    concat(r: Result): Result;
    toString(): string;
}
